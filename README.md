# ABOUT
My portfolio website containing my software as well as design projects + a bit more about me!

# TOOLS USED
Designed on Figma, starter template components built using builder.io; Frameworks include React, Vite, Tailwind and languages used are Typescript, HTML and CSS; Deployment (CI/CD) on Vercel

# USAGE
**Prerequisites**: Node.js/pnpm/npm

Run the following commands in the order:

```bash
pnpm install/npm install
npm run build
npm start


Then navigate to http://localhost:3000

